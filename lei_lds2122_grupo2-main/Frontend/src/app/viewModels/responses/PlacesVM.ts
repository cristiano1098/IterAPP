
/**
 * View Model that represents a list of places IDs
 */
export class PlacesVM {

    /**
     * Construct's an instance of {@link PlacesVM}
     * 
     * @param places list of palces ids
     */
    constructor(
        public places?: Array<string>
    ) { }
}