
/**
 * Interface to convert json objects to string
 */
export interface toJSONString {

    /**
     * Converts the class into a string representation of a json object 
     * 
     * @returns string representation of the object
     */
    toJSONString(): string
}